diff -ru a/config.m4 b/config.m4
--- a/config.m4	2026-09-12 05:46:10.665283800 -0400
+++ b/config.m4	2026-09-12 05:46:10.781306600 -0400
@@ -78,20 +78,17 @@
       -L$CMARK_DIR/$PHP_LIBDIR -lm
     ])
 
+    dnl Patched for kirigami/php-wasm-compiler (CLAUDE.md decision 34):
+    dnl upstream's AC_TRY_RUN here actually EXECUTES the compiled test
+    dnl program, which is impossible when cross-compiling to wasm32 (no
+    dnl fourth "action-if-cross-compiling" argument is given, so unpatched
+    dnl this hard-fails with "cannot run test program while cross
+    dnl compiling"). We always vendor a known, fixed libcmark version (see
+    dnl compile/libcmark/Dockerfile + matrix.json's "cmark" library entry)
+    dnl already verified to satisfy this minimum, so the runtime probe is
+    dnl skipped rather than faked with a cached autoconf answer.
     AC_MSG_CHECKING([for cmark minimal version])
-    old_CFLAGS=$CFLAGS
-    CFLAGS="-I$CMARK_DIR/include"
-    AC_TRY_RUN([
-#include <cmark.h>
-int main() {
-  return (CMARK_VERSION < (($LIBCMARK_MIN_MAJOR << 16) | ($LIBCMARK_MIN_MINOR << 8)) ? 1 : 0);
-}
-    ], [
-      AC_MSG_RESULT([ok])
-    ], [
-      AC_MSG_ERROR(system libcmark is too old: version 0.28 required)
-    ])
-    CFLAGS=$old_CFLAGS
+    AC_MSG_RESULT([skipped (cross-compiling; version pinned by the vendored libcmark build)])
   fi
 
   PHP_SUBST(CMARK_SHARED_LIBADD)
diff -ru a/src/block.c b/src/block.c
--- a/src/block.c	2026-09-12 05:46:10.674283800 -0400
+++ b/src/block.c	2026-09-12 05:46:10.791372900 -0400
@@ -41,6 +41,7 @@
 	((php_cmark_node_code_block_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_code_block_t, h.h.std)))
 #define php_cmark_node_code_block_fetch(z) php_cmark_node_code_block_from(Z_OBJ_P(z))
+#define php_cmark_node_code_block_fetch_obj(o) php_cmark_node_code_block_from(o)
 
 zend_object* php_cmark_node_code_block_create(zend_class_entry *ce) {
 	php_cmark_node_code_block_t *n = 
@@ -55,100 +56,65 @@
 	return php_cmark_node_zend(&n->h.h);
 }
 
-zval* php_cmark_node_code_block_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_code_block_read_error;
-	}
+zval* php_cmark_node_code_block_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_fence_info))
-			return php_cmark_node_read_str(&n->h.h, 
+			return php_cmark_node_read_str(&n->h.h,
 				cmark_node_get_fence_info, &n->fence, rv);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "fence")) {
-		return php_cmark_node_read_str(&n->h.h, 
+	if (zend_string_equals_literal(member, "fence")) {
+		return php_cmark_node_read_str(&n->h.h,
 			RTS(rtc, cmark_node_get_fence_info), &n->fence, rv);
 	}
 
-php_cmark_node_code_block_read_error:
 	return php_cmark_node_text_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_code_block_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_code_block_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch(object);
+zval* php_cmark_node_code_block_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_fence_info)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
+			php_cmark_assert_type(value, IS_STRING, 0,
+				return &EG(uninitialized_zval),
 				"fence expected to be string");
 
-			php_cmark_node_write_str(&n->h.h, 
+			php_cmark_node_write_str(&n->h.h,
 				cmark_node_set_fence_info, value, &n->fence);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
-		}	
+		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "fence")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"fence expected to be string");
-
-			php_cmark_node_write_str(&n->h.h, 
-				RTS(rtc, cmark_node_set_fence_info), value, &n->fence);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "fence")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+			return &EG(uninitialized_zval),
+			"fence expected to be string");
+
+		php_cmark_node_write_str(&n->h.h,
+			RTS(rtc, cmark_node_set_fence_info), value, &n->fence);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
     return php_cmark_node_text_write(object, member, value, rtc);
-#else
-	php_cmark_node_text_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_code_block_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch(object);
+int php_cmark_node_code_block_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_fence_info)) {
-			zv = php_cmark_node_read_str(&n->h.h, 
+			zv = php_cmark_node_read_str(&n->h.h,
 				cmark_node_get_fence_info, &n->fence, NULL);
 			goto php_cmark_node_code_block_isset_result;
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "fence")) {
-		zv = php_cmark_node_read_str(&n->h.h, 
+	if (zend_string_equals_literal(member, "fence")) {
+		zv = php_cmark_node_read_str(&n->h.h,
 			RTS(rtc, cmark_node_get_fence_info), &n->fence, NULL);
 	}
 
@@ -160,28 +126,23 @@
 	return php_cmark_node_text_isset(object, member, has_set_exists, rtc);
 }
 
-void php_cmark_node_code_block_unset(zval *object, zval *member, void **rtc) {
-	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_code_block_unset_error;
-	}
+void php_cmark_node_code_block_unset(zend_object *object, zend_string *member, void **rtc) {
+	php_cmark_node_code_block_t *n = php_cmark_node_code_block_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_fence_info)) {
-			php_cmark_node_write_str(&n->h.h, 
+			php_cmark_node_write_str(&n->h.h,
 				cmark_node_set_fence_info, NULL, &n->fence);
 			return;
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "fence")) {
-		php_cmark_node_write_str(&n->h.h, 
+	if (zend_string_equals_literal(member, "fence")) {
+		php_cmark_node_write_str(&n->h.h,
 			RTS(rtc, cmark_node_set_fence_info), NULL, &n->fence);
 		return;
 	}
 
-php_cmark_node_code_block_unset_error:
 	return php_cmark_node_text_unset(object, member, rtc);
 }
 
diff -ru a/src/call.c b/src/call.c
--- a/src/call.c	2026-09-12 05:46:10.721645900 -0400
+++ b/src/call.c	2026-09-12 05:46:10.823785100 -0400
@@ -56,9 +56,9 @@
 	zend_object_std_dtor(zo);
 }
 
-static inline zend_object* php_cmark_call_clone(zval *zv) {
-	zend_class_entry *type = Z_OBJCE_P(zv);
-	php_cmark_call_t *src = php_cmark_call_fetch(zv);
+static inline zend_object* php_cmark_call_clone(zend_object *zo) {
+	zend_class_entry *type = zo->ce;
+	php_cmark_call_t *src = php_cmark_call_fetch_obj(zo);
 	php_cmark_call_t *dst = (php_cmark_call_t*) 
 		ecalloc(1, sizeof(php_cmark_call_t) + 
 			zend_object_properties_size(type));
diff -ru a/src/call.h b/src/call.h
--- a/src/call.h	2026-09-12 05:46:10.726264000 -0400
+++ b/src/call.h	2026-09-12 05:46:10.822271300 -0400
@@ -27,6 +27,7 @@
 	((php_cmark_call_t*) \
 		((char*) o - XtOffsetOf(php_cmark_call_t, std)))
 #define php_cmark_call_fetch(z) php_cmark_call_from(Z_OBJ_P(z))
+#define php_cmark_call_fetch_obj(o) php_cmark_call_from(o)
 
 extern zend_class_entry *php_cmark_call_ce;
 
diff -ru a/src/custom.c b/src/custom.c
--- a/src/custom.c	2026-09-12 05:46:10.681285200 -0400
+++ b/src/custom.c	2026-09-12 05:46:10.843377300 -0400
@@ -42,12 +42,8 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_custom_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_custom_read_error;
-	}
+zval* php_cmark_node_custom_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_on_enter))
@@ -58,10 +54,10 @@
 				cmark_node_get_on_exit, &n->onLeave, rv);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "onEnter")) {
+	if (zend_string_equals_literal(member, "onEnter")) {
 		return php_cmark_node_read_str(&n->h, 
 			RTS(rtc, cmark_node_get_on_enter), &n->onEnter, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "onLeave")) {
+	} else if (zend_string_equals_literal(member, "onLeave")) {
 		return php_cmark_node_read_str(&n->h, 
 			RTS(rtc, cmark_node_get_on_exit), &n->onLeave, rv);
 	}
@@ -70,96 +66,50 @@
 	return php_cmark_node_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_custom_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_custom_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch(object);
+zval* php_cmark_node_custom_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_on_enter)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
+			php_cmark_assert_type(value, IS_STRING, 0,
+				return &EG(uninitialized_zval),
 				"onEnter expected to be string");
-			php_cmark_node_write_str(&n->h, 
+			php_cmark_node_write_str(&n->h,
 				cmark_node_set_on_enter, value, &n->onEnter);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		} else if (RTC(rtc, cmark_node_set_on_exit)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
+			php_cmark_assert_type(value, IS_STRING, 0,
+				return &EG(uninitialized_zval),
 				"onLeave expected to be string");
-			php_cmark_node_write_str(&n->h, 
+			php_cmark_node_write_str(&n->h,
 				cmark_node_set_on_exit, value, &n->onLeave);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
-		}	
+		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "onEnter")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"onEnter expected to be string");
-			php_cmark_node_write_str(&n->h, 
-				RTS(rtc, cmark_node_set_on_enter), value, &n->onEnter);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		} else if (zend_string_equals_literal(Z_STR_P(member), "onLeave")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"onLeave expected to be string");
-			php_cmark_node_write_str(&n->h, 
-				RTS(rtc, cmark_node_set_on_exit), value, &n->onLeave);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "onEnter")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+			return &EG(uninitialized_zval),
+			"onEnter expected to be string");
+		php_cmark_node_write_str(&n->h,
+			RTS(rtc, cmark_node_set_on_enter), value, &n->onEnter);
+		return value;
+	} else if (zend_string_equals_literal(member, "onLeave")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+			return &EG(uninitialized_zval),
+			"onLeave expected to be string");
+		php_cmark_node_write_str(&n->h,
+			RTS(rtc, cmark_node_set_on_exit), value, &n->onLeave);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
     return php_cmark_node_write(object, member, value, rtc);
-#else
-	php_cmark_node_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_custom_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch(object);
+int php_cmark_node_custom_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_on_enter)) {
 			zv = php_cmark_node_read_str(&n->h, 
@@ -172,10 +122,10 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "onEnter")) {
+	if (zend_string_equals_literal(member, "onEnter")) {
 		zv = php_cmark_node_read_str(&n->h, 
 			RTS(rtc, cmark_node_get_on_enter), &n->onEnter, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "onLeave")) {
+	} else if (zend_string_equals_literal(member, "onLeave")) {
 		zv = php_cmark_node_read_str(&n->h, 
 			RTS(rtc, cmark_node_get_on_exit), &n->onLeave, NULL);
 	}
@@ -188,12 +138,8 @@
 	return php_cmark_node_isset(object, member, has_set_exists, rtc);
 }
 
-void php_cmark_node_custom_unset(zval *object, zval *member, void **rtc) {
-	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_custom_unset_error;
-	}
+void php_cmark_node_custom_unset(zend_object *object, zend_string *member, void **rtc) {
+	php_cmark_node_custom_t *n = php_cmark_node_custom_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_on_enter)) {
@@ -207,11 +153,11 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "onEnter")) {
+	if (zend_string_equals_literal(member, "onEnter")) {
 		php_cmark_node_write_str(&n->h, 
 			RTS(rtc, cmark_node_set_on_enter), NULL, &n->onEnter);
 		return;
-	} else if (zend_string_equals_literal(Z_STR_P(member), "onLeave")) {
+	} else if (zend_string_equals_literal(member, "onLeave")) {
 		php_cmark_node_write_str(&n->h, 
 			RTS(rtc, cmark_node_set_on_exit), NULL, &n->onLeave);
 		return;
diff -ru a/src/custom.h b/src/custom.h
--- a/src/custom.h	2026-09-12 05:46:10.682284300 -0400
+++ b/src/custom.h	2026-09-12 05:46:10.807230100 -0400
@@ -28,6 +28,8 @@
 	((php_cmark_node_custom_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_custom_t, h.std)))
 #define php_cmark_node_custom_fetch(z) php_cmark_node_custom_from(Z_OBJ_P(z))
+/* CLAUDE.md decision 34: see php_cmark_node_fetch_obj() in node.h. */
+#define php_cmark_node_custom_fetch_obj(o) php_cmark_node_custom_from(o)
 
 extern zend_object* php_cmark_node_custom_create(zend_class_entry *ce);
 
diff -ru a/src/handlers.c b/src/handlers.c
--- a/src/handlers.c	2026-09-12 05:46:10.685711000 -0400
+++ b/src/handlers.c	2026-09-12 05:46:10.808230200 -0400
@@ -139,12 +139,8 @@
 	return rv;
 }
 
-zval* php_cmark_node_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_t *n = php_cmark_node_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_read_error;
-	}
+zval* php_cmark_node_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_t *n = php_cmark_node_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_parent))
@@ -176,66 +172,51 @@
 				cmark_node_get_end_column, &n->endColumn);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "parent")) {
+	if (zend_string_equals_literal(member, "parent")) {
 		return php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_parent), &n->parent, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "previous")) {
+	} else if (zend_string_equals_literal(member, "previous")) {
 		return php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_previous), &n->previous, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "next")) {
+	} else if (zend_string_equals_literal(member, "next")) {
 		return php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_next), &n->next, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "firstChild")) {
+	} else if (zend_string_equals_literal(member, "firstChild")) {
 		return php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_first_child), &n->firstChild, rv);
-	}  else if (zend_string_equals_literal(Z_STR_P(member), "lastChild")) {
+	}  else if (zend_string_equals_literal(member, "lastChild")) {
 		return php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_last_child), &n->lastChild, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "startLine")) {
+	} else if (zend_string_equals_literal(member, "startLine")) {
 		return php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_start_line), &n->startLine);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "endLine")) {
+	} else if (zend_string_equals_literal(member, "endLine")) {
 		return php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_end_line), &n->endLine);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "startColumn")) {
+	} else if (zend_string_equals_literal(member, "startColumn")) {
 		return php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_start_column), &n->startColumn);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "endColumn")) {
+	} else if (zend_string_equals_literal(member, "endColumn")) {
 		return php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_end_column), &n->endColumn);
 	} else {
-php_cmark_node_read_error:
 		php_cmark_throw(
-			"invalid read of %s", 
-			Z_TYPE_P(member) == IS_STRING ? 
-				Z_STRVAL_P(member) : "invalid property");
+			"invalid read of %s", ZSTR_VAL(member));
 	}
 
-	return &EG(uninitialized_zval);	
+	return &EG(uninitialized_zval);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
+zval* php_cmark_node_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
 	php_cmark_throw(
-		"invalid write of %s", 
-		Z_TYPE_P(member) == IS_STRING ? 
-			Z_STRVAL_P(member) : "invalid property");
-#if PHP_VERSION_ID >= 70400
+		"invalid write of %s", ZSTR_VAL(member));
     return &EG(uninitialized_zval);
-#endif
 }
 
-int php_cmark_node_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_t *n = php_cmark_node_fetch(object);
+int php_cmark_node_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_t *n = php_cmark_node_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_parent)) {
 			zv = php_cmark_node_read_object(n, 
@@ -276,31 +257,31 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "parent")) {
+	if (zend_string_equals_literal(member, "parent")) {
 		zv = php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_parent), &n->parent, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "previous")) {
+	} else if (zend_string_equals_literal(member, "previous")) {
 		zv = php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_previous), &n->previous, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "next")) {
+	} else if (zend_string_equals_literal(member, "next")) {
 		zv = php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_next), &n->next, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "firstChild")) {
+	} else if (zend_string_equals_literal(member, "firstChild")) {
 		zv = php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_first_child), &n->firstChild, NULL);
-	}  else if (zend_string_equals_literal(Z_STR_P(member), "lastChild")) {
+	}  else if (zend_string_equals_literal(member, "lastChild")) {
 		zv = php_cmark_node_read_object(n, 
 			RTS(rtc, cmark_node_last_child), &n->lastChild, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "startLine")) {
+	} else if (zend_string_equals_literal(member, "startLine")) {
 		zv = php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_start_line), &n->startLine);
-	}  else if (zend_string_equals_literal(Z_STR_P(member), "endLine")) {
+	}  else if (zend_string_equals_literal(member, "endLine")) {
 		zv = php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_end_line), &n->endLine);
-	}  else if (zend_string_equals_literal(Z_STR_P(member), "startColumn")) {
+	}  else if (zend_string_equals_literal(member, "startColumn")) {
 		zv = php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_start_column), &n->startColumn);
-	}   else if (zend_string_equals_literal(Z_STR_P(member), "endColumn")) {
+	}   else if (zend_string_equals_literal(member, "endColumn")) {
 		zv = php_cmark_node_read_int(n, 
 			RTS(rtc, cmark_node_get_end_column), &n->endColumn);
 	}
@@ -308,11 +289,9 @@
 	return Z_TYPE_P(zv) == IS_OBJECT || Z_TYPE_P(zv) == IS_LONG;
 }
 
-void php_cmark_node_unset(zval *object, zval *member, void **rtc) {
+void php_cmark_node_unset(zend_object *object, zend_string *member, void **rtc) {
 	php_cmark_throw(
-		"invalid write of %s", 
-		Z_TYPE_P(member) == IS_STRING ? 
-			Z_STRVAL_P(member) : "invalid property");
+		"invalid write of %s", ZSTR_VAL(member));
 }
 
 static inline cmark_node* php_cmark_node_clone_impl(cmark_node *source);
@@ -483,21 +462,21 @@
 	zend_hash_str_update(debug, "children", sizeof("children")-1, &children);
 }
 
-HashTable* php_cmark_node_debug(zval *zv, int *tmp) {
+HashTable* php_cmark_node_debug(zend_object *zo, int *tmp) {
 	HashTable *debug;
 
 	ALLOC_HASHTABLE(debug);
 	zend_hash_init(debug, 8, NULL, ZVAL_PTR_DTOR, 0);
 
-	php_cmark_node_debug_impl(debug, php_cmark_node_fetch(zv));
+	php_cmark_node_debug_impl(debug, php_cmark_node_fetch_obj(zo));
 
 	*tmp = 1;
 
 	return debug;
 }
 
-zend_object* php_cmark_node_clone(zval *zv) {
-	php_cmark_node_t *zo = php_cmark_node_fetch(zv);
+zend_object* php_cmark_node_clone(zend_object *obj) {
+	php_cmark_node_t *zo = php_cmark_node_fetch_obj(obj);
 	php_cmark_node_t *co = 
 		(php_cmark_node_t*) 
 			ecalloc(1, sizeof(php_cmark_node_t));
diff -ru a/src/handlers.h b/src/handlers.h
--- a/src/handlers.h	2026-09-12 05:46:10.686723300 -0400
+++ b/src/handlers.h	2026-09-12 05:46:10.836691000 -0400
@@ -18,8 +18,11 @@
 #ifndef PHP_CMARK_NODE_HANDLERS_H
 #define PHP_CMARK_NODE_HANDLERS_H
 
-extern HashTable* php_cmark_node_debug(zval *zv, int *tmp);
-extern zend_object* php_cmark_node_clone(zval *zv);
+/* CLAUDE.md decision 34: migrated from PHP 7's zval*-based object handler
+ * ABI to PHP 8's zend_object* / zend_string*-based one — upstream krakjoe/
+ * cmark (last real commit 2019) predates this PHP 8 change entirely. */
+extern HashTable* php_cmark_node_debug(zend_object *zo, int *tmp);
+extern zend_object* php_cmark_node_clone(zend_object *zo);
 extern void php_cmark_node_free(zend_object *zo);
 
 typedef int (*cmark_node_read_int) (cmark_node*);
@@ -35,14 +38,10 @@
 void php_cmark_node_write_int(php_cmark_node_t *n, cmark_node_write_int cmark_node_write, zval *value, zval *cache);
 void php_cmark_node_write_bool(php_cmark_node_t *n, cmark_node_write_int cmark_node_write, zval *value, zval *cache);
 
-extern zval* php_cmark_node_read(zval *object, zval *member, int type, void **rtc, zval *rv);
-#if PHP_VERSION_ID >= 70400
-extern zval* php_cmark_node_write(zval *object, zval *member, zval *value, void **rtc);
-#else
-extern void php_cmark_node_write(zval *object, zval *member, zval *value, void **rtc);
-#endif
-extern int php_cmark_node_isset(zval *object, zval *member, int has_set_exists, void **rtc);
-extern void php_cmark_node_unset(zval *object, zval *member, void **rtc);
+extern zval* php_cmark_node_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv);
+extern zval* php_cmark_node_write(zend_object *object, zend_string *member, zval *value, void **rtc);
+extern int php_cmark_node_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc);
+extern void php_cmark_node_unset(zend_object *object, zend_string *member, void **rtc);
 
 #define RTS(c, f) ((c) ? (*c) = (f) : (f))
 #define RTC(c, f) ((*c) == (f))
diff -ru a/src/heading.c b/src/heading.c
--- a/src/heading.c	2026-09-12 05:46:10.687837800 -0400
+++ b/src/heading.c	2026-09-12 05:46:10.815230400 -0400
@@ -37,6 +37,7 @@
 	((php_cmark_node_heading_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_heading_t, h.std)))
 #define php_cmark_node_heading_fetch(z) php_cmark_node_heading_from(Z_OBJ_P(z))
+#define php_cmark_node_heading_fetch_obj(o) php_cmark_node_heading_from(o)
 
 zend_object* php_cmark_node_heading_create(zend_class_entry *ce) {
 	php_cmark_node_heading_t *n = 
@@ -51,90 +52,55 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_heading_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_heading_read_error;
-	}
+zval* php_cmark_node_heading_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_heading_level))
-			return php_cmark_node_read_int(&n->h, 
+			return php_cmark_node_read_int(&n->h,
 				cmark_node_get_heading_level, &n->level);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "level")) {
-		return php_cmark_node_read_int(&n->h, 
+	if (zend_string_equals_literal(member, "level")) {
+		return php_cmark_node_read_int(&n->h,
 			RTS(rtc, cmark_node_get_heading_level), &n->level);
 	}
 
-php_cmark_node_heading_read_error:
 	return php_cmark_node_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_heading_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_heading_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch(object);
+zval* php_cmark_node_heading_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_heading_level)) {
 			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
+				value, IS_LONG, 0,
+				return &EG(uninitialized_zval),
                 "level expected to be int");
-			php_cmark_node_write_int(&n->h, 
+			php_cmark_node_write_int(&n->h,
 				cmark_node_set_heading_level, value, &n->level);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
-		}	
+		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "level")) {
-			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-                "level expected to be int");
-			php_cmark_node_write_int(&n->h, 
-				RTS(rtc, cmark_node_set_heading_level), value, &n->level);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "level")) {
+		php_cmark_assert_type(
+			value, IS_LONG, 0,
+			return &EG(uninitialized_zval),
+            "level expected to be int");
+		php_cmark_node_write_int(&n->h,
+			RTS(rtc, cmark_node_set_heading_level), value, &n->level);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
 	return php_cmark_node_write(object, member, value, rtc);
-#else
-	php_cmark_node_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_heading_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch(object);
+int php_cmark_node_heading_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_heading_t *n = php_cmark_node_heading_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_heading_level)) {
 			zv = php_cmark_node_read_int(&n->h, 
@@ -143,7 +109,7 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "level")) {
+	if (zend_string_equals_literal(member, "level")) {
 		zv = php_cmark_node_read_int(&n->h, 
 			RTS(rtc, cmark_node_get_heading_level), &n->level);
 	}
diff -ru a/src/list.c b/src/list.c
--- a/src/list.c	2026-09-12 05:46:10.691406000 -0400
+++ b/src/list.c	2026-09-12 05:46:10.821229900 -0400
@@ -47,6 +47,7 @@
 	((php_cmark_node_list_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_list_t, h.std)))
 #define php_cmark_node_list_fetch(z) php_cmark_node_list_from(Z_OBJ_P(z))
+#define php_cmark_node_list_fetch_obj(o) php_cmark_node_list_from(o)
 
 zend_object* php_cmark_node_list_create(zend_class_entry *ce) {
 	php_cmark_node_list_t *n = 
@@ -61,12 +62,8 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_list_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_list_t *n = php_cmark_node_list_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_list_read_error;
-	}
+zval* php_cmark_node_list_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_list_t *n = php_cmark_node_list_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_list_tight))
@@ -77,112 +74,65 @@
 				(cmark_node_read_int) cmark_node_get_list_delim, &n->delimiter);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "tight")) {
+	if (zend_string_equals_literal(member, "tight")) {
 		return php_cmark_node_read_bool(&n->h, (cmark_node_read_int) 
 			RTS(rtc, cmark_node_get_list_tight), &n->tight);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "delimiter")) {
+	} else if (zend_string_equals_literal(member, "delimiter")) {
 		return php_cmark_node_read_int(&n->h, (cmark_node_read_int) 
 			RTS(rtc, cmark_node_get_list_delim), &n->delimiter);
 	}
 
-php_cmark_node_list_read_error:
 	return php_cmark_node_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_list_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_list_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_list_t *n = php_cmark_node_list_fetch(object);
+zval* php_cmark_node_list_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_list_t *n = php_cmark_node_list_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_list_tight)) {
 			php_cmark_assert_type(
-				value, _IS_BOOL, 0, 
-#if PHP_VERSION_ID >= 70400
+				value, _IS_BOOL, 0,
 			    return &EG(uninitialized_zval),
-#else
-			    return,
-#endif
                 "tight expected to be bool");
-			php_cmark_node_write_bool(&n->h, 
+			php_cmark_node_write_bool(&n->h,
 				(cmark_node_write_int) cmark_node_set_list_tight, value, &n->tight);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		} else if (RTC(rtc, cmark_node_set_list_delim)) {
 			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
+				value, IS_LONG, 0,
 			    return &EG(uninitialized_zval),
-#else
-			    return,
-#endif
                 "delimiter expected to be int");
-			php_cmark_node_write_int(&n->h, 
+			php_cmark_node_write_int(&n->h,
 				(cmark_node_write_int) cmark_node_set_list_delim, value, &n->delimiter);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "tight")) {
-			php_cmark_assert_type(
-				value, _IS_BOOL, 0, 
-#if PHP_VERSION_ID >= 70400
-			    return &EG(uninitialized_zval),
-#else
-			    return,
-#endif
-                "tight expected to be bool");
-			php_cmark_node_write_bool(&n->h, 
-				(cmark_node_write_int) RTS(rtc, cmark_node_set_list_tight), value, &n->tight);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		} else if (zend_string_equals_literal(Z_STR_P(member), "delimiter")) {
-			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
-			    return &EG(uninitialized_zval),
-#else
-			    return,
-#endif
-                "delimiter expected to be int");
-			php_cmark_node_write_int(&n->h, 
-				(cmark_node_write_int) RTS(rtc, cmark_node_set_list_delim), value, &n->delimiter);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "tight")) {
+		php_cmark_assert_type(
+			value, _IS_BOOL, 0,
+		    return &EG(uninitialized_zval),
+            "tight expected to be bool");
+		php_cmark_node_write_bool(&n->h,
+			(cmark_node_write_int) RTS(rtc, cmark_node_set_list_tight), value, &n->tight);
+		return value;
+	} else if (zend_string_equals_literal(member, "delimiter")) {
+		php_cmark_assert_type(
+			value, IS_LONG, 0,
+		    return &EG(uninitialized_zval),
+            "delimiter expected to be int");
+		php_cmark_node_write_int(&n->h,
+			(cmark_node_write_int) RTS(rtc, cmark_node_set_list_delim), value, &n->delimiter);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
-    return 	php_cmark_node_write(object, member, value, rtc);
-#else
-	php_cmark_node_write(object, member, value, rtc);
-#endif
+    return php_cmark_node_write(object, member, value, rtc);
 }
 
-int php_cmark_node_list_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_list_t *n = php_cmark_node_list_fetch(object);
+int php_cmark_node_list_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_list_t *n = php_cmark_node_list_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_list_tight)) {
 			zv = php_cmark_node_read_bool(&n->h, 
@@ -195,10 +145,10 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "tight")) {
+	if (zend_string_equals_literal(member, "tight")) {
 		zv = php_cmark_node_read_bool(&n->h, 
 			(cmark_node_read_int) RTS(rtc, cmark_node_get_list_tight), &n->tight);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "delimiter")) {
+	} else if (zend_string_equals_literal(member, "delimiter")) {
 		zv = php_cmark_node_read_int(&n->h, 
 			(cmark_node_read_int) RTS(rtc, cmark_node_get_list_delim), &n->delimiter);
 	}
@@ -224,6 +174,7 @@
 	((php_cmark_node_ordered_list_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_ordered_list_t, h.std)))
 #define php_cmark_node_ordered_list_fetch(z) php_cmark_node_ordered_list_from(Z_OBJ_P(z))
+#define php_cmark_node_ordered_list_fetch_obj(o) php_cmark_node_ordered_list_from(o)
 
 zend_object* php_cmark_node_ordered_list_create(zend_class_entry *ce) {
 	php_cmark_node_ordered_list_t *n = 
@@ -238,90 +189,55 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_ordered_list_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_ordered_list_read_error;
-	}
+zval* php_cmark_node_ordered_list_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_list_start))
-			return php_cmark_node_read_int(&n->h, 
+			return php_cmark_node_read_int(&n->h,
 				(cmark_node_read_int) cmark_node_get_list_start, &n->start);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "start")) {
-		return php_cmark_node_read_int(&n->h, 
+	if (zend_string_equals_literal(member, "start")) {
+		return php_cmark_node_read_int(&n->h,
 			(cmark_node_read_int) RTS(rtc, cmark_node_get_list_start), &n->start);
 	}
 
-php_cmark_node_ordered_list_read_error:
 	return php_cmark_node_list_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_ordered_list_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_ordered_list_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch(object);
+zval* php_cmark_node_ordered_list_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_list_start)) {
 			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
+				value, IS_LONG, 0,
                 return &EG(uninitialized_zval),
-#else
-                return,
-#endif
                 "start expected to be int");
-			php_cmark_node_write_int(&n->h, 
+			php_cmark_node_write_int(&n->h,
 				(cmark_node_write_int) cmark_node_set_list_start, value, &n->start);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "start")) {
-			php_cmark_assert_type(
-				value, IS_LONG, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-                "start expected to be int");
-			php_cmark_node_write_int(&n->h, 
-				(cmark_node_write_int) RTS(rtc, cmark_node_set_list_start), value, &n->tight);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "start")) {
+		php_cmark_assert_type(
+			value, IS_LONG, 0,
+            return &EG(uninitialized_zval),
+            "start expected to be int");
+		php_cmark_node_write_int(&n->h,
+			(cmark_node_write_int) RTS(rtc, cmark_node_set_list_start), value, &n->tight);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
     return php_cmark_node_list_write(object, member, value, rtc);
-#else
-	php_cmark_node_list_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_ordered_list_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch(object);
+int php_cmark_node_ordered_list_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_ordered_list_t *n = php_cmark_node_ordered_list_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_list_start)) {
 			zv = php_cmark_node_read_int(&n->h, 
@@ -330,7 +246,7 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "start")) {
+	if (zend_string_equals_literal(member, "start")) {
 		zv = php_cmark_node_read_int(&n->h, (cmark_node_read_int) 
 			RTS(rtc, cmark_node_get_list_start), &n->start);
 	}
diff -ru a/src/media.c b/src/media.c
--- a/src/media.c	2026-09-12 05:46:10.691406000 -0400
+++ b/src/media.c	2026-09-12 05:46:10.791372900 -0400
@@ -39,6 +39,7 @@
 	((php_cmark_node_media_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_media_t, h.std)))
 #define php_cmark_node_media_fetch(z) php_cmark_node_media_from(Z_OBJ_P(z))
+#define php_cmark_node_media_fetch_obj(o) php_cmark_node_media_from(o)
 
 zend_object* php_cmark_node_media_create(zend_class_entry *ce) {
 	php_cmark_node_media_t *n = 
@@ -53,12 +54,8 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_media_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_media_t *n = php_cmark_node_media_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_media_read_error;
-	}
+zval* php_cmark_node_media_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_media_t *n = php_cmark_node_media_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_url))
@@ -67,125 +64,78 @@
 			return php_cmark_node_read_str(&n->h, cmark_node_get_title, &n->title, rv);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "url")) {
-		return php_cmark_node_read_str(&n->h, 
+	if (zend_string_equals_literal(member, "url")) {
+		return php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_url), &n->url, rv);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "title")) {
-		return php_cmark_node_read_str(&n->h, 
+	} else if (zend_string_equals_literal(member, "title")) {
+		return php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_title), &n->title, rv);
 	}
 
-php_cmark_node_media_read_error:
 	return php_cmark_node_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_media_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_media_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_media_t *n = php_cmark_node_media_fetch(object);
+zval* php_cmark_node_media_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_media_t *n = php_cmark_node_media_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_url)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
+			php_cmark_assert_type(value, IS_STRING, 0,
                 return &EG(uninitialized_zval),
-#else
-                return,
-#endif
                 "url expected to be string");
-			php_cmark_node_write_str(&n->h, 
+			php_cmark_node_write_str(&n->h,
 				cmark_node_set_url, value, &n->url);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		} else if (RTC(rtc, cmark_node_set_title)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
+			php_cmark_assert_type(value, IS_STRING, 0,
                 return &EG(uninitialized_zval),
-#else
-                return,
-#endif
 				"title expected to be string");
-			php_cmark_node_write_str(&n->h, 
+			php_cmark_node_write_str(&n->h,
 				cmark_node_set_title, value, &n->title);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
 		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "url")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"url expected to be string");
-			php_cmark_node_write_str(&n->h, 
-				RTS(rtc, cmark_node_set_url), value, &n->url);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		} else if (zend_string_equals_literal(Z_STR_P(member), "title")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"title expected to be string");
-			php_cmark_node_write_str(&n->h, 
-				RTS(rtc, cmark_node_set_title), value, &n->title);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "url")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+            return &EG(uninitialized_zval),
+			"url expected to be string");
+		php_cmark_node_write_str(&n->h,
+			RTS(rtc, cmark_node_set_url), value, &n->url);
+		return value;
+	} else if (zend_string_equals_literal(member, "title")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+            return &EG(uninitialized_zval),
+			"title expected to be string");
+		php_cmark_node_write_str(&n->h,
+			RTS(rtc, cmark_node_set_title), value, &n->title);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
     return php_cmark_node_write(object, member, value, rtc);
-#else
-	php_cmark_node_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_media_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_media_t *n = php_cmark_node_media_fetch(object);
+int php_cmark_node_media_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_media_t *n = php_cmark_node_media_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_url)) {
-			zv = php_cmark_node_read_str(&n->h, 
+			zv = php_cmark_node_read_str(&n->h,
 				cmark_node_get_url, &n->url, NULL);
 			goto php_cmark_node_media_isset_result;
 		} else if (RTC(rtc, cmark_node_get_title)) {
-			zv = php_cmark_node_read_str(&n->h, 
+			zv = php_cmark_node_read_str(&n->h,
 				cmark_node_get_title, &n->title, NULL);
 			goto php_cmark_node_media_isset_result;
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "url")) {
-		zv = php_cmark_node_read_str(&n->h, 
+	if (zend_string_equals_literal(member, "url")) {
+		zv = php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_url), &n->url, NULL);
-	} else if (zend_string_equals_literal(Z_STR_P(member), "title")) {
-		zv = php_cmark_node_read_str(&n->h, 
+	} else if (zend_string_equals_literal(member, "title")) {
+		zv = php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_title), &n->title, NULL);
 	}
 
@@ -197,12 +147,8 @@
 	return php_cmark_node_isset(object, member, has_set_exists, rtc);
 }
 
-void php_cmark_node_media_unset(zval *object, zval *member, void **rtc) {
-	php_cmark_node_media_t *n = php_cmark_node_media_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_media_unset_error;
-	}
+void php_cmark_node_media_unset(zend_object *object, zend_string *member, void **rtc) {
+	php_cmark_node_media_t *n = php_cmark_node_media_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_title)) {
@@ -214,11 +160,11 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "title")) {
+	if (zend_string_equals_literal(member, "title")) {
 		php_cmark_node_write_str(&n->h, 
 			RTS(rtc, cmark_node_set_title), NULL, &n->title);
 		return;
-	} else if (zend_string_equals_literal(Z_STR_P(member), "url")) {
+	} else if (zend_string_equals_literal(member, "url")) {
 		php_cmark_node_write_str(&n->h, 
 			RTS(rtc, cmark_node_set_url), NULL, &n->url);
 		return;
diff -ru a/src/node.h b/src/node.h
--- a/src/node.h	2026-09-12 05:46:10.704237800 -0400
+++ b/src/node.h	2026-09-12 05:46:10.843377300 -0400
@@ -49,6 +49,10 @@
 	((php_cmark_node_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_t, std)))
 #define php_cmark_node_fetch(z) php_cmark_node_from(Z_OBJ_P(z))
+/* CLAUDE.md decision 34: PHP 8's object handlers (read_property et al.)
+ * receive a zend_object* directly instead of a zval* — this fetches from
+ * that zend_object* without going through Z_OBJ_P(). */
+#define php_cmark_node_fetch_obj(o) php_cmark_node_from(o)
 #define php_cmark_node_zend(z) ((zend_object*) &(z)->std)
 
 extern PHP_MINIT_FUNCTION(CommonMark_Node);
diff -ru a/src/text.c b/src/text.c
--- a/src/text.c	2026-09-12 05:46:10.709237400 -0400
+++ b/src/text.c	2026-09-12 05:46:10.789822700 -0400
@@ -39,6 +39,7 @@
 	((php_cmark_node_text_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_text_t, h.std)))
 #define php_cmark_node_text_fetch(z) php_cmark_node_text_from(Z_OBJ_P(z))
+#define php_cmark_node_text_fetch_obj(o) php_cmark_node_text_from(o)
 
 zend_object* php_cmark_node_text_create(zend_class_entry *ce) {
 	php_cmark_node_text_t *n = 
@@ -53,98 +54,63 @@
 	return php_cmark_node_zend(&n->h);
 }
 
-zval* php_cmark_node_text_read(zval *object, zval *member, int type, void **rtc, zval *rv) {
-	php_cmark_node_text_t *n = php_cmark_node_text_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_text_read_error;
-	}
+zval* php_cmark_node_text_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv) {
+	php_cmark_node_text_t *n = php_cmark_node_text_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_literal))
 			return php_cmark_node_read_str(&n->h, cmark_node_get_literal, &n->literal, rv);
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "literal")) {
-		return php_cmark_node_read_str(&n->h, 
+	if (zend_string_equals_literal(member, "literal")) {
+		return php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_literal), &n->literal, rv);
 	}
 
-php_cmark_node_text_read_error:
 	return php_cmark_node_read(object, member, type, rtc, rv);
 }
 
-#if PHP_VERSION_ID >= 70400
-zval* php_cmark_node_text_write(zval *object, zval *member, zval *value, void **rtc) {
-#else
-void php_cmark_node_text_write(zval *object, zval *member, zval *value, void **rtc) {
-#endif
-	php_cmark_node_text_t *n = php_cmark_node_text_fetch(object);
+zval* php_cmark_node_text_write(zend_object *object, zend_string *member, zval *value, void **rtc) {
+	php_cmark_node_text_t *n = php_cmark_node_text_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_literal)) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
+			php_cmark_assert_type(value, IS_STRING, 0,
+				return &EG(uninitialized_zval),
 				"literal expected to be string");
 
 			php_cmark_node_write_str(&n->h, cmark_node_set_literal, value, &n->literal);
-#if PHP_VERSION_ID >= 70400
 			return value;
-#else
-			return;
-#endif
-		}	
+		}
 	}
 
-	if (Z_TYPE_P(member) == IS_STRING) {
-		if (zend_string_equals_literal(Z_STR_P(member), "literal")) {
-			php_cmark_assert_type(value, IS_STRING, 0, 
-#if PHP_VERSION_ID >= 70400
-                return &EG(uninitialized_zval),
-#else
-                return,
-#endif
-				"literal expected to be string");
-
-			php_cmark_node_write_str(&n->h, 
-				RTS(rtc, cmark_node_set_literal), value, &n->literal);
-#if PHP_VERSION_ID >= 70400
-			return value;
-#else
-			return;
-#endif
-		}
+	if (zend_string_equals_literal(member, "literal")) {
+		php_cmark_assert_type(value, IS_STRING, 0,
+			return &EG(uninitialized_zval),
+			"literal expected to be string");
+
+		php_cmark_node_write_str(&n->h,
+			RTS(rtc, cmark_node_set_literal), value, &n->literal);
+		return value;
 	}
 
-#if PHP_VERSION_ID >= 70400
     return php_cmark_node_write(object, member, value, rtc);
-#else
-	php_cmark_node_write(object, member, value, rtc);
-#endif
 }
 
-int php_cmark_node_text_isset(zval *object, zval *member, int has_set_exists, void **rtc) {
-	php_cmark_node_text_t *n = php_cmark_node_text_fetch(object);
+int php_cmark_node_text_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc) {
+	php_cmark_node_text_t *n = php_cmark_node_text_fetch_obj(object);
 	zval *zv = &EG(uninitialized_zval);
 
-	if (Z_TYPE_P(member) != IS_STRING) {
-		return 0;
-	}
-
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_get_literal)) {
-			zv = php_cmark_node_read_str(&n->h, 
+			zv = php_cmark_node_read_str(&n->h,
 				RTS(rtc, cmark_node_get_literal), &n->literal, NULL);
 			goto php_cmark_node_text_isset_result;
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "literal")) {
-		zv = php_cmark_node_read_str(&n->h, 
+	if (zend_string_equals_literal(member, "literal")) {
+		zv = php_cmark_node_read_str(&n->h,
 			RTS(rtc, cmark_node_get_literal), &n->literal, NULL);
 	}
 
@@ -156,12 +122,8 @@
 	return php_cmark_node_isset(object, member, has_set_exists, rtc);
 }
 
-void php_cmark_node_text_unset(zval *object, zval *member, void **rtc) {
-	php_cmark_node_text_t *n = php_cmark_node_text_fetch(object);
-
-	if (Z_TYPE_P(member) != IS_STRING) {
-		goto php_cmark_node_text_unset_error;
-	}
+void php_cmark_node_text_unset(zend_object *object, zend_string *member, void **rtc) {
+	php_cmark_node_text_t *n = php_cmark_node_text_fetch_obj(object);
 
 	if (EXPECTED(rtc)) {
 		if (RTC(rtc, cmark_node_set_literal)) {
@@ -170,13 +132,12 @@
 		}
 	}
 
-	if (zend_string_equals_literal(Z_STR_P(member), "literal")) {
-		php_cmark_node_write_str(&n->h, 
+	if (zend_string_equals_literal(member, "literal")) {
+		php_cmark_node_write_str(&n->h,
 			RTS(rtc, cmark_node_set_literal), NULL, &n->literal);
 		return;
 	}
 
-php_cmark_node_text_unset_error:
 	return php_cmark_node_unset(object, member, rtc);
 }
 
diff -ru a/src/text.h b/src/text.h
--- a/src/text.h	2026-09-12 05:46:10.726264000 -0400
+++ b/src/text.h	2026-09-12 05:46:10.818229900 -0400
@@ -36,13 +36,11 @@
 	((php_cmark_node_text_t*) \
 		((char*) o - XtOffsetOf(php_cmark_node_text_t, h.std)))
 #define php_cmark_node_text_fetch(z) php_cmark_node_text_from(Z_OBJ_P(z))
+#define php_cmark_node_text_fetch_obj(o) php_cmark_node_text_from(o)
 
-extern int php_cmark_node_text_isset(zval *object, zval *member, int has_set_exists, void **rtc);
-#if PHP_VERSION_ID >= 70400
-extern zval* php_cmark_node_text_write(zval *object, zval *member, zval *value, void **rtc);
-#else
-extern void php_cmark_node_text_write(zval *object, zval *member, zval *value, void **rtc);
-#endif
-extern zval* php_cmark_node_text_read(zval *object, zval *member, int type, void **rtc, zval *rv);
-extern void php_cmark_node_text_unset(zval *object, zval *member, void **rtc);
+/* CLAUDE.md decision 34: PHP 8 object handler ABI (zend_object* / zend_string*). */
+extern int php_cmark_node_text_isset(zend_object *object, zend_string *member, int has_set_exists, void **rtc);
+extern zval* php_cmark_node_text_write(zend_object *object, zend_string *member, zval *value, void **rtc);
+extern zval* php_cmark_node_text_read(zend_object *object, zend_string *member, int type, void **rtc, zval *rv);
+extern void php_cmark_node_text_unset(zend_object *object, zend_string *member, void **rtc);
 #endif
Only in b: vendor
