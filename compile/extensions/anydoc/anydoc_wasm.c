/*
 * Intentionally empty (not upstream): the anydoc extension, get_module
 * included, lives in the Rust static archive linked into this side module
 * (vendorLib anydoc, built by compile/anydoc/Dockerfile). phpize only needs
 * one C source to build a module from. See PROVENANCE.md.
 */
typedef int anydoc_wasm_translation_unit_is_not_empty;
